import React from 'react';

import Layout from '../components/Layout';

const NotFound = () => {
  return (
    <Layout containerTitle="404: Page not found">
      <p>Sorry, we couldn't find anything like that.</p>
    </Layout>
  );
};

export default NotFound;
